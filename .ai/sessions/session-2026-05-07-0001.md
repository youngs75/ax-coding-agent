# 세션 핸드오프 — 2026-05-07-0001: POST /workspace/reset endpoint 구현

## 프로젝트 개요
ax-coding-agent (LangGraph 기반 multi-agent coding harness) 포털 운영 지속.
이 세션은 apt-web 채팅 UI 의 "워크스페이스 초기화" 버튼 연동을 위해
ax-coding-agent 측에 `POST /workspace/reset` endpoint 를 구현한 작업.

## 작업 환경
- **실행 위치**: VDI Claude Code (포털 Web IDE 연동 SSOT 작업)
- **GitLab**: `gitlab.samsungsdscoe.com/74435f2f-2053-4d88-b00e-55e2b2d92bf0/ax-coding-agent.git`
- **GitHub**: `github.com/youngs75/ax-coding-agent.git`
- **배포 endpoint**: `https://portal-serving-evangelist-1-agent2-e4e8d185.samsungsdscoe.com`

## 이전 세션 대비 sync 작업

세션 시작 시 GitHub 에 다음 커밋들이 앞서 있어 GitLab 에 순차 동기화함:

| SHA | 내용 |
|---|---|
| `c317449` | fix(web): sse_emitter chat UI todo panel + HITL modal 회귀 |
| `74e1f65` | fix(web): sse_emitter ALB keep-alive + langgraph event 진단 logging |
| `ac8f5e0` | fix(web): sse_emitter invoke.end role 추적 (last_role state) |
| `62819a8` | docs(handoff): 2026-04-30-0001 |
| `8ff5ccc` | fix(web): sse_emitter write_todos/update_todo todo.change 발행 회귀 |
| `74e3977` | fix(web): sse_emitter task_tool.auto_advance status 갱신 wire |
| `6309253` | feat(web): verifier/fixer auto-chain chat UI surface |
| `e22135b` ~ `10d63f7` | v22.4 harness fix + Mimo provider + R-003 위반 폐기 (VDI 작업분) |

## 이번 세션 주요 작업

### POST /workspace/reset 구현 (`7113664`)

apt-web 작업지시서(`# ax-coding-agent 작업 요청 — 워크스페이스 초기화 endpoint 구현`)
에 따라 구현.

**변경 파일**:
- `coding_agent/web/artifacts.py` — `reset_workspace()` 함수 추가
  - `_resolve_workspace()` 로 workspace 경로 확인
  - top-level entry 순회 → `_is_excluded_path()` 통과한 항목만 삭제
  - 디렉토리는 `shutil.rmtree`, 파일은 `unlink`
  - `.git`, `.venv`, `node_modules` 등 excluded dir 보존
  - 오류 시 error 목록 반환 (500), 정상 시 `{"status": "ok", "message": "workspace cleared"}` (200)
  - 멱등 — 빈 workspace 에 호출해도 200

- `coding_agent/web/app.py` — `POST /workspace/reset` 라우트 등록
  - `reset_workspace()` import 추가
  - 예외 캐치 → 500 반환

- `coding_agent/web/agent_card.py` — `endpoints.workspaceReset` 추가
  ```json
  "workspaceReset": "https://<host>/workspace/reset"
  ```

- `tests/web/test_artifacts.py` — reset 관련 테스트 4개 추가
  - `test_agent_card_advertises_workspace_reset`
  - `test_workspace_reset_clears_user_files`
  - `test_workspace_reset_preserves_excluded_dirs`
  - `test_workspace_reset_is_idempotent`

**테스트 결과**: `tests/web/` 51/51 passed

### merge 처리

구현 커밋(`7113664`) 후 GitHub main 에 VDI 작업 7개 커밋이 앞서 있어
`git merge --no-edit github/main` → merge commit `a060024` 생성.
양쪽 repo 최종 HEAD = `a060024`.

## 현재 상태

### 리포 동기화
| 원격 | HEAD |
|---|---|
| GitHub | `a060024` |
| GitLab | `a060024` |

### 미배포
포털 재배포 필요 (GitLab push 완료, Deploy 재트리거는 사용자 수동).

## apt-web 측 연동 안내

```
endpoint:     POST /workspace/reset
응답 형식:    {"status": "ok", "message": "workspace cleared"}   (200)
              {"status": "error", "message": "<사유>"}           (500)
agent card:   endpoints.workspaceReset = "https://<host>/workspace/reset"
커밋:         7113664
```

apt-web 작업지시서 §2 에 따라 apt-web 측에서 진행할 사항:
1. `chat/router.py` — `POST /coding/a2a/workspace/reset` → ax relay 추가
2. `chat_coding.html` — "🗑 워크스페이스 초기화" 버튼 + 확인 다이얼로그 추가

## 다음 단계

1. **포털 재배포** — 사용자가 포털 UI 에서 Deploy 재트리거
2. **apt-web 연동** — apt-web Claude Code 가 §2 작업 수행 후 E2E 검증
3. **검증 커맨드**:
   ```bash
   AX_URL="https://portal-serving-evangelist-1-agent2-e4e8d185.samsungsdscoe.com"
   curl -s -X POST "$AX_URL/workspace/reset" | python3 -m json.tool
   curl -s "$AX_URL/.well-known/agent.json" | python3 -c \
     "import sys,json; print(json.dumps(json.load(sys.stdin)['endpoints'], indent=2))"
   ```

## 중요 참고사항

- `POST /workspace/reset` 은 `AX_ARTIFACTS_DIR` env (없으면 `config.project_root`) 기준으로 workspace 경로 결정
- excluded dir (`.git`, `.venv`, `node_modules`, `__pycache__`, `dist`, `build` 등) 은 삭제 대상에서 제외 — 에이전트 실행 환경 보존
- GitHub ↔ GitLab 동기화 패턴: `git fetch github` → `git merge --ff-only github/main` (FF 불가 시 `--no-edit`) → `git push origin main`
