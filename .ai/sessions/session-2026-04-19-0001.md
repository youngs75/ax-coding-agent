# Session Handoff — 2026-04-19-0001

## 프로젝트 개요

AX Coding Agent — 3계층 장기 메모리, 동적 SubAgent 수명주기, Agentic Loop 복원력을 갖춘 AI Coding Agent Harness.

**이번 세션의 성격 (리팩터링 세션)**: 2026-04-12 과제 제출 이후 동결 상태였던 이 repo 를 **`minyoung-mah` 라이브러리의 세 번째 소비자**로 재포지셔닝. harness 5책임(Safety/Detection/Clarity/Context/Observation)을 라이브러리로 이관하고, ax 내부에는 coding-domain 특화 부분(6 role prompts, file/shell tools, LangGraph 최상위 ReAct 루프, CLI, 3-tier memory extractor)만 남기는 대규모 리팩토링 수행.

이미 apt-legal-agent, prime-jennie-runtime 2개 소비자가 라이브러리를 쓰고 있던 상태에서, ax 도 동일 구조로 정렬하여 이후 library ↔ 소비자 간 상호 진화가 가능하도록 만듬.

## 작업 범위 및 완료 상태

**옵션 B (주력 교체) 9-Phase 계획, 전원 완료**. 플랜 파일: `/home/youngs75/.claude/plans/resilient-imagining-sedgewick.md`.

### 3개 commit 으로 푸시 완료 (`origin/main` 기준)

| commit | Phase | LOC 변화 |
|---|---|---|
| `9e2bc48` | Phase 1–4 (의존성 + resilience + memory store + tools adapter) | −1416 / +914 |
| `904662d` | Phase 5–6 (roles + Orchestrator + task_tool 재배선 + Manager/Registry/Factory/Models 삭제) | −2781 / +1260 |
| `4e9adb2` | Phase 7–9 (Observer composite + 테스트 추가 + 문서 재포지셔닝 + 제출 artefact 제거) | −187 / +389 |
| **Net** | | **−3801 LOC** |

Push 대상 remote: `https://github.com/youngs75/ax-coding-agent.git` (gh CLI 의 active 계정을 `youngs7596` → `youngs75` 로 전환한 뒤 성공).

## 리팩터 내역 (Phase 별)

### Phase 1 — minyoung-mah 의존성 추가
- `pyproject.toml` dependencies 에 `"minyoung-mah>=0.1.2"` 추가
- `Makefile install` 타겟이 `pip install -e ../minyoung-mah` 를 선행하도록 확장 (apt-legal-agent 패턴)

### Phase 2 — resilience/* 제거 → minyoung_mah.resilience
- 삭제: `coding_agent/resilience/` 디렉토리 전체 (6개 파일)
- 신설: `coding_agent/resilience_compat.py` — Watchdog / SafeStop / ErrorHandler / ErrorClassifier / FailureType / DEFAULT_POLICIES 를 기존 구현에서 그대로 이관
- `core/loop.py` 에 `_task_id_extractor` 추가 — TASK-NN secondary-key extractor 를 library `ProgressGuard.key_extractor` 에 주입
- `ProgressGuard` 는 library 소유, ax 는 `GuardVerdict` 와 함께 compat 경유로 re-export

### Phase 3 — memory/store.py 제거 → SqliteMemoryStore
- 삭제: `coding_agent/memory/store.py` (~250 LOC)
- `memory/schema.py` 에 `MemoryRecord ↔ minyoung_mah.MemoryEntry` 변환 헬퍼 (`record_to_entry`, `entry_to_record`) 추가. category/source 는 metadata{json} 에 담김
- `memory/middleware.py` 를 **async** 로 재작성 (library store 가 async). `_list_tier` 는 library `_conn` 직접 접근으로 tier/scope enumerate (library 가 `list_all` 을 제공하지 않기 때문)
- LangGraph 노드 `inject_memory` / `extract_memory` 를 `async def` 로 승격
- DB 스키마 비호환 → `memory_store/memory.db` → `memory_store/ax.v2.db` 로 경로 이동 (.env / .env.example / config.py)

### Phase 4 — tools → ToolAdapter 프로토콜 적응
- 신설: `coding_agent/tools/adapters.py` — `_LangChainToolAdapter` 가 기존 StructuredTool 을 `minyoung_mah.ToolAdapter` 로 래핑. sync 도구는 `asyncio.to_thread` 로 offload. `FILE_ADAPTERS` / `SHELL_ADAPTERS` export.
- 기존 LangChain StructuredTool 경로는 유지 (LangGraph 최상위 agent_node 는 무변경)

### Phase 5 — role 정의 이동 (factory.py → roles.py + orchestrator_factory.py)
- 신설: `coding_agent/subagents/roles.py` — 6 role 을 `@dataclass(frozen=True) CodingAgentRole` 로 정의 (duck-types `minyoung_mah.SubAgentRole`). `build_user_message` 에서 user_decisions header + memory_snippets + parent_outputs["previous_ask"] 주입
- 신설: `coding_agent/subagents/orchestrator_factory.py` — `build_orchestrator()` 가 ToolRegistry(file/shell/ask adapter) + RoleRegistry(6 roles) + TieredModelRouter + SqliteMemoryStore + NullHITLChannel + StructlogObserver + default_resilience 를 조립
- 신설: `coding_agent/subagents/user_decisions.py` — UserDecisionsLog (중복 제거 + `header()` 메서드)
- 신설: `coding_agent/subagents/classifier.py` — factory._analyze_task 를 `classify_task` / `resolve_role_name` 로 독립

### Phase 6 — task_tool 재배선 + Manager/Registry/Factory/Models 삭제
- **삭제**: `coding_agent/subagents/{manager.py, registry.py, factory.py, models.py}` (~2,000 LOC)
- `tools/task_tool.py` 완전 재작성 — `SubAgentManager.spawn` → `orchestrator.invoke_role(role_name, InvocationContext(...))`
- 신설: `coding_agent/tools/ask_adapter.py` — `AskUserQuestionAdapter` 가 langgraph `interrupt()` 대신 `ToolResult.value = {"__ax_interrupt__": True, "payload": ...}` 마커 리턴
- task_tool 이 role COMPLETED 후 `_find_pending_interrupt()` 로 마커 검출 → LangGraph `interrupt(payload)` 호출 → 사용자 답변을 `parent_outputs["previous_ask"]` 로 재주입하면서 `orchestrator.invoke_role` 재호출 (plan §결정 3)
- `auto_advance_todo` 는 `_auto_advance_todo` task_tool 내부 helper 로 이관
- verifier `execute(command, result)` 페어링 포맷팅도 task_tool 레벨로 이관 (`_format_verifier_output`)
- `core/loop.py` AgentLoop: SubAgent machinery 3줄 → `build_orchestrator(...)` + `TodoStore` + `UserDecisionsLog` 직접 소유. `get_registry()` 제거, `get_orchestrator()` / `get_todo_store()` / `set_todo_change_callback()` 신설
- `cli/app.py`: `/agents`, `/events` 는 observer 로 이전 중이라 안내만 출력. `/memory` 는 async `SqliteMemoryStore` API 로 재작성

### Phase 7 — Observer 전환 (Structlog + Langfuse composite)
- 신설: `coding_agent/observability/langfuse_observer.py` — `LangfuseForwardObserver` 가 Orchestrator 의 `orchestrator.role.invoke.*` / `orchestrator.pipeline.step.*` span 을 Langfuse span 으로 매핑. env 키 없거나 SDK 초기화 실패 시 no-op
- `build_default_observer()` → `CompositeObserver(StructlogObserver, LangfuseForward)` (Langfuse 활성 시) 또는 StructlogObserver 단독

### Phase 8 — 테스트 정리
- 삭제: `tests/test_subagents.py`, `test_p35_regressions.py`, `test_interrupt_integration.py` (manager/registry/factory 기반)
- `test_resilience.py` 의 ProgressGuard 섹션 제거 (라이브러리가 소유)
- `test_p35_phase3.py` 재작성 — manager 의존 제거, `_auto_advance_todo` helper 테스트 추가
- `test_todo_tool.py`, `test_ask_tool.py`, `test_performance.py` 의 manager 참조 섹션 수정/제거
- 신설: `test_user_decisions.py` (8 tests), `test_ask_adapter.py` (6 tests)
- Makefile: `test-subagents` → `test-roles` 타겟 교체
- **결과**: 235 passed → 187 passed (차이는 라이브러리로 이전된 단위 테스트)

### Phase 9 — 제출 artefact 제거 + 문서 재포지셔닝
- 삭제: `make-submission.sh`
- README.md: 상단에 "minyoung-mah 라이브러리 소비자" positioning 블록 추가
- AGENTS.md: 프로젝트 구조 + 3축 설명 + 테스트 타겟을 새 구조 기준으로 재작성 (resilience_compat, observability, minyoung_mah 소유 모듈 명시)

## Cross-cutting 결정 (플랜 §결정)

1. **Memory 주입**: 정적 system_prompt + `build_user_message` 의 `invocation.memory_snippets` 경유. prompt 캐시 히트 유지 + apt-legal 패턴 일치.
2. **Tier fallback**: 최상위 LangGraph `handle_error` 노드 + `resilience_compat.ErrorHandler` 에 그대로 유지. 라이브러리 ModelRouter 계약 건드리지 않음.
3. **HITL interrupt**: role 내부에서 `interrupt()` 직접 호출 안 함. `AskUserQuestionAdapter` 마커 → task_tool 에서 LangGraph interrupt → `parent_outputs["previous_ask"]` 재주입.
4. **ProgressGuard 이중 기록 방지**: 최상위 agent_node 의 guard 는 top-level task 호출만 기록. Orchestrator 내부 guard 는 `default_resilience` 기본값인 `disabled` 사용 (각 role 은 `max_iterations=100` bounded).

## 🔴 다음 세션 ToDo (최우선)

### E2E 테스트 결과 분석
사용자가 이번 세션 종료 시점에 E2E 테스트를 시작함. 다음 세션에서 다음을 확인:

1. **성공/실패 판정**
   - 완주했는지, 중간 exit 이 있었는지
   - exit_reason 추적 (ProgressGuard stop / consecutive_errors / safe_stop / fatal_error)

2. **v9 baseline 과 비교** (EVIDENCE.md §8 참조)
   - v9: 35.0분 / 15 task / 26 SubAgent / verifier 38s / fixer 1회 / HITL 3문항
   - ±20% 이내면 Acceptance 통과

3. **회귀 핫스팟 5개 검증** (플랜 §3):
   - **인터럽트 재배선** — `AskUserQuestionAdapter` 마커 → `task_tool.interrupt()` → `parent_outputs["previous_ask"]` 재주입이 실제 HITL 흐름에서 작동하는지. planner 가 ask 후 answer 받고 PRD 를 처음부터 다시 쓰지 않는지 (중복 작업 손실). `grep "task_tool.propagate_interrupt"` / `task_tool.received_answer` 로그 확인
   - **Memory async 승격** — LangGraph `InMemorySaver` 가 async inject_memory/extract_memory 노드를 정상 체크포인트하는지. 2nd turn 에서 user preference 가 `<agent_memory><user>` 블록에 나타나는지
   - **Verifier 출력 포맷** — verifier → fixer 전환 시 task_tool 의 `_format_verifier_output` 이 `execute(command, result)` pair 를 노출하는지. fixer 가 구체적 에러를 받는지
   - **User decisions 전파** — `ask_user_question` 답변이 `UserDecisionsLog` 에 기록되고, 후속 coder/verifier/fixer 의 `build_user_message` 에 `"## 사용자 결정 사항 (하드 제약)"` 블록이 prepend 되는지
   - **TASK-NN progress guard** — 동일 TASK-NN 6회 재위임 시 최상위 guard 가 STOP 내리는지 (v8 핫픽스 유지 여부). `grep "progress_guard.record"` 가 여러 건 + `progress_guard.secondary_repeat_warn/stop` 이 필요시 찍히는지

4. **실측 데이터 수집**
   ```bash
   # SubAgent invoke 집계
   python3 -c "
   import re
   from collections import defaultdict
   log = '/tmp/<workspace>/.ax-agent/logs/agent.log'
   role_times = defaultdict(list)
   for l in open(log):
       if 'orchestrator.role.invoke.end' in l:
           r = re.search(r\"role='(\w+)'\", l)
           d = re.search(r'duration_ms=(\d+)', l)
           if r and d:
               role_times[r.group(1)].append(int(d.group(1))/1000)
   for role, t in sorted(role_times.items()):
       print(f'{role:12s} n={len(t):3d} sum={sum(t):7.1f}s max={max(t):6.1f}s')
   "

   # 또는 기존 timing.subagent.invoke 대신 library 의 새 span 이벤트 기반
   grep -c 'orchestrator.role.invoke.start' /tmp/<workspace>/.ax-agent/logs/agent.log
   ```

5. **문제 발견 시 핫픽스 후보**
   - 회귀 핫스팟 어디서 터졌는지 파악 → 플랜 §결정의 트레이드오프가 여전히 옳은지 재평가
   - 필요하면 라이브러리에도 gap 을 PR 로 피드백 (apt-legal 이 Phase 2b 에서 했던 방식)

## 현재 코드 상태

**워킹 디렉토리**: clean (3 commits 푸시 완료)

**브랜치**: `main` @ `4e9adb2`

**테스트**: 187/187 통과 (Phase 8 기준)

**Python 의존성**: `minyoung-mah==0.1.2` (editable at `/home/youngs75/projects/minyoung-mah`)

**DB**: `memory_store/ax.v2.db` (기존 `memory.db` 는 v1 스키마로 유휴 상태)

## 중요 참고사항

### minyoung-mah 라이브러리 버전 호환
- 현재 설치: `0.1.2` (editable)
- 만약 라이브러리 쪽에서 breaking change 가 발생하면 `minyoung-mah/CHANGELOG.md` 확인
- `Orchestrator`, `ProgressGuard.key_extractor`, `SubAgentRole` protocol, `ToolAdapter.call` 시그니처가 현재 사용 중인 표면

### resilience_compat.py 의 운명
- Watchdog/SafeStop/ErrorHandler/ErrorClassifier/retry_with_backoff 는 ax-specific (tier fallback 정책은 최상위 LangGraph 에서만 의미가 있음)
- 라이브러리에 유사 기능이 들어오면 이 compat 을 slim 하거나 삭제 검토
- 현재는 최상위 agent_node `handle_error` 가 `ErrorHandler` 를 사용하므로 삭제 불가

### 삭제 대상이지만 남긴 것
- `coding_agent/core/tool_adapter.py` + `tool_call_utils.py` — GLM-5 등 native tool calling 미지원 모델용 프롬프트 기반 tool 호출 폴백. ax-specific 이고 library 에 해당 기능 없음. 유지.
- `coding_agent/core/orchestrator.py` — should_delegate 등 top-level orchestration helper. LangGraph agent_node 가 쓰므로 유지. (이름 충돌 주의: minyoung_mah.Orchestrator 와 이름이 같지만 다른 것.)

### 커밋/푸시 계정 주의
- gh CLI 에 두 계정(`youngs7596`, `youngs75`) 모두 로그인돼 있음
- 이 repo 는 **youngs75** 계정으로 push (사용자 명시)
- 세션 시작 시 `gh auth status` 로 active 계정 확인 권장
- 메모리: `feedback_git_account.md`

### 플랜 파일
- `/home/youngs75/.claude/plans/resilient-imagining-sedgewick.md`
- 리팩터 전체 설계 + 결정 기록. 다음 세션 회귀 분석 시 §결정/§핫스팟 참조.

### 동시에 진행 중일 수 있는 작업
- `minyoung-mah` 라이브러리 자체 업데이트 (다른 세션에서)
- `apt-legal-agent`, `prime-jennie-runtime` 소비자들의 use-case 가 library 에 새 기능을 요구할 수 있음
- 다음 세션 시작 시 `git -C ../minyoung-mah log --oneline -5` 로 라이브러리 최근 commit 확인 권장

## E2E 재현 명령 (참고)

```bash
# 1. LiteLLM Gateway 기동
make down && make up

# 2. 빈 워크스페이스에서 에이전트 실행
rm -rf /tmp/new_pms_qwen3_v10/* && ./ax-agent.sh /tmp/new_pms_qwen3_v10

# 3. PMS 요구사항 입력 후 HITL 답변

# 4. 종료 후 measurement (위의 "실측 데이터 수집" 참조)
```

## 시간 추세 (PMS 동일 입력, 역사적 기록)

| 라운드 | 시간 | task | 상태 |
|---|---|---|---|
| v4 | 14.7분 | 8 | 완료 |
| v5 | 30분 | 12 | 완료 |
| v6 | 24.8분 | 14 | 가장 효율 |
| v7 | (cancel) | - | 무한 reject (Sub-B trigger) |
| v8 | 76분+ | 25 | cancel (4 핫픽스 발견) |
| v9 | 35.0분 | 15 | 완주 + 4 핫픽스 실증 |
| **v10** | **진행 중** | **?** | **minyoung-mah consumer 재배선 후 첫 E2E** |
