# 세션 핸드오프 — 2026-05-02-0001: v27 e2e + R-003 위반 일괄 폐기

## 프로젝트 개요

전 핸드오프 (`session-2026-05-01-0001.md`) 직후 v27 워크스페이스에서
Mimo + v22.4 통합 e2e 실행. 24 + 10 task 모두 PASS 했지만 sufficiency
critic_escalate 가 cycle 감지 (PRD 커버리지 35% LOW verdict 반복). 진단
결과 *35% 가 산출물 누락이 아니라 keyword 추출기의 noise* 였고, 더
근본적으로는 **PRD ↔ 산출물 정합성을 정규식/substring/임계값 으로
판단하려 한 코드 자체가 R-003 (format coercion 으로 robustness 메우기
금지) 정면 위반**이었음. 사용자가 원칙 위반 직접 catch → 두 위반 사례
일괄 폐기 + ratchet 메모리 강화.

## 작업 환경

`session-2026-05-01-0001.md` 의 작업 (v22.4 4-처방 + Mimo provider) 위에
두 commit 추가. 이미지는 v22.4 + Mimo 베이크 후 변동 없음 (R-003 폐기
는 다음 e2e 빌드 시점에 반영 예정).

## 한 일

### 1. v27 e2e 실행 결과 분석 — Mimo + v22.4 검증 ✓ + 새 신호 catch

`/home/youngs75/temp/ax_pms_v27/` 에서 PMS (프로젝트 관리 시스템) 의도
의 4.3시간 (15388.9s) e2e:

- **24 task ✓ + 10 task ✓** (planner 가 PRD coverage 보강 위해 추가 분해
  TASK-10.1~10.10 하고 모두 처리)
- **Mimo 호환성** ✓ — `mimo-v2.5-pro` tool calling, multi-turn,
  reasoning_content preserve 모두 안정
- **v22.4 #1 효과** ✓ — `DONE_CONDITION.md` 가 *완벽히 깨끗*. 자연어
  조건어 0개, pure glob only (planner skill 가이드 효과 입증)
- **v22.4 #3 효과** — fixer cap 발화 자체 없음 (verifier 모두 PASS)
- **v22.4 #4 효과** ✓ — 모든 task chain advance 정상, verify_failed 0건
- **sufficiency cycle detection** ✓ — iteration=2 같은 verdict 반복 →
  `critic_escalate` (정확히 의도대로)

`prd_coverage=0.35` 가 LOW verdict 반복의 직접 사유.

### 2. 첫 진단 시도 — *증상* 만 보고 잘못 진단 (사용자 catch)

`_extract_prd_keywords` 의 정규식이 한국어 PRD 의 헤더+bullet 을 합쳐
keyword 26/40 misses 만든 것을 확인하고 *추출기 noise* 라고 진단.
v22.5 큐 후보로 "regex 개선" 제시. 사용자가 정확히 catch:

> "아직도 정규식으로 뭔가를 검증하는 로직이 남아있어? 패턴 검사,
>  정규식 검사 이런걸로는 제대로 동작하기 어렵다는 것을 우리는 6개의
>  문서를 읽고 확인했잖아. 판단이 필요하다면 LLM 을 호출한다."

→ 6편 통합 분석 (`.ai/research/harness-insights-2026-04-26.md`) 의 6번째
책임 + R-003 ratchet 의 정의를 그대로 위반. 정규식 *개선* 이 아니라
*폐기* 가 정답.

### 3. R-003 위반 코드 일괄 폐기 (commit `b27b66a`)

**(a) `_compute_prd_coverage`** — 이전부터 존재하던 위반.
- `_read_prd` / `_extract_prd_keywords` / `_scan_workspace_text` /
  `_compute_prd_coverage` *전부* `signals.py` 에서 제거
- `rules.py`: `high_prd` / `low_prd` 파라미터 + LOW/HIGH/MEDIUM 의 PRD
  분기 + heuristic_verdict 의 PRD-only 분기 모두 제거
- `config.py`: `sufficiency_high_prd` / `sufficiency_low_prd` 필드 제거
- `core/loop.py` + `cli/display.py` + `critic.py`: `prd_coverage` 참조 정리

**(b) `_extract_forbidden_patterns` 의 자연어 키워드 거부 (v22.4 #2)** —
직전 commit (`841106e`) 에서 *내가 추가한* 위반.
- `_NL_CONDITION_RE` / `_PAREN_NOTE_RE` 정규식 + 자연어 거부 분기 제거
- planner skill (`done-condition.md`) 의 "안전망 발화" 표현 제거 — 가이드
  는 LLM 행동 지침으로만 유지
- `tests/test_done_condition.py` 의 자연어 거부 5 테스트 제거

**테스트 영향**:
- 433 → 422 (11 테스트 제거: PRD 분기 6 + 자연어 거부 5)
- 회귀 0 — `pytest tests/` PASS

### 4. R-003 ratchet 메모리 강화

`feedback_ratchet_rules.md` 의 R-003 본문에 *확장 정의* 추가:

- v22.4 #2 위반 사례 (a) 와 `_compute_prd_coverage` 위반 사례 (b)
  명시
- **결정론 vs 판단 경계** 명문화:
  - 결정론 영역: 파일 존재, glob 매치, exit code, 정수 카운트 → 정규식
    OK
  - 판단 영역: 의미 동치성, 의도 추출, "충분한가" 판정 → *항상* LLM
  - 회색 지대 (예: 사용자 요청 키워드) 는 *명시적 trade-off* 주석 필수
- "내가 직접 어긴 케이스" 로 향후 재발 차단

## 현재 상태

| Repo | Local | Remote |
|---|---|---|
| ax-coding-agent | `main` `b27b66a` | `b27b66a` ✓ (push 완료) |
| 작업트리 | clean | — |
| Docker 이미지 | v22.4 + Mimo (R-003 폐기 미반영) | — |

본 세션 commit:

| # | Commit | 메시지 |
|---|---|---|
| 1 | `b27b66a` | fix(sufficiency): R-003 위반 코드 폐기 — _compute_prd_coverage + 자연어 거부 정규식 |
| 2 | `<NEW>` | docs(handoff): 본 세션 파일 |

본 세션 *전* 의 직전 commit:

| # | Commit | 메시지 |
|---|---|---|
| - | `9dfee42` | chore(ax-agent): MIMO_API_KEY/MIMO_BASE_URL forward env 추가 |
| - | `361ceb9` | docs(handoff): 세션 핸드오프 2026-05-01-0001 |
| - | `3ca7513` | feat(models): Xiaomi Mimo v2.5 provider 통합 |
| - | `841106e` | feat(harness): v22.4 — DONE_CONDITION 자연어 거부 + ... |

## 미해결 + 다음 단계

### A. v27 ax-agent CLI 종료 처리 (사용자 작업)

v27 컨테이너는 *이전 빌드* (R-003 폐기 미반영) 상태로 `[You] >` prompt
대기 중. cycle 감지 후 critic_escalate 발화한 상태:
- 옵션 1: "OK 종료" 입력 → 정상 종료. 산출물 (24+10 task) 은 워크스페이스
  보존
- 옵션 2: 추가 요청 → 어차피 같은 LOW verdict cycle 재발

권장: 옵션 1.

### B. v28 e2e — R-003 폐기 효과 직접 검증 ★

```bash
docker buildx build --build-context minyoung_mah=../minyoung-mah \
  -t ax-coding-agent .
mkdir -p /home/youngs75/temp/ax_pms_v28
cd /home/youngs75/temp/ax_pms_v28
~/projects/ax_advanced_coding_ai_agent/ax-agent.sh
```

검증 포인트:
1. **PRD coverage false-positive 제거** — sufficiency.gate 가 prd_coverage
   신호 없이도 정상 동작 (HIGH/MEDIUM/LOW 분기). 이전처럼 35% LOW
   무한 cycle 안 발화
2. **DONE_CONDITION 자연어 안전망 제거 효과** — planner skill 가이드만
   으로 깨끗한 forbidden pattern 작성되는지 (v27 에서 이미 검증됐지만
   안전망 없이도 유지되는지)
3. **critic LLM 의 PRD ↔ 산출물 판단** — MEDIUM 분기에서 critic 이
   raw text 로 의미적 정합성 판단. 현재 critic prompt 는 metrics +
   user_request 만 첨부 — *PRD 본문 + 워크스페이스 파일 트리 첨부*
   강화는 v22.5 후속 작업

### C. v22.5 후속 — critic prompt 에 PRD/산출물 본문 첨부

`coding_agent/sufficiency/critic.py` 의 `_render_critic_summary` 가
metrics 만 첨부. PRD 본문 + 워크스페이스 파일 트리 (read_file 도구 호출
힌트와 함께) 를 첨부해 critic LLM 이 *직접* PRD ↔ 산출물 정합성 판단
가능하도록. v28 e2e 결과 보고 우선순위 결정.

## 중요 참고사항

### R-003 의 진짜 메타-실수

내가 v22.4 #2 (자연어 거부 정규식) 를 *처음 도입한* 게 R-003 위반이고,
v27 진단에서도 *증상 (regex noise)* 만 보고 *처방으로 regex 개선* 제시
한 게 두 번째 위반. 사용자가 원칙 명시적으로 환기시켜 두 사례 모두
catch.

R-003 의 *확장 정의* 가 메모리에 추가됨 — 향후 회귀 진단 시 "정규식/
substring/임계값으로 처리할 수 있나?" 질문 자체를 *거꾸로* 해야 함:
"이게 판단 영역인가? 그러면 LLM 이다."

### v27 e2e 가 검증한 것

|  v22.4 처방 | 효과 |
|---|---|
| #1 (planner skill) | ✓ DONE_CONDITION.md *완벽히 깨끗*, 24+10 task 모두 정합 |
| #2 (자연어 거부) | — 발화 자체 없음 (#1 만으로 충분 입증). R-003 위반으로 폐기 |
| #3 (fixer gate cap) | — fixer 호출 자체 없음 (verifier 모두 PASS) |
| #4 (verify_failed) | ✓ chain advance 정상, verify_failed 0 |

→ #2 는 dead code 였고, #1 가이드만으로 sufficient. 향후 *원칙* 검토를
처방 도입 *전* 단계에 의무화.

### Mimo 호환성 입증

4.3시간 안정 동작, tool calling 정상, multi-turn 회귀 없음, fast tier
의 reasoning_tokens 비용도 acceptable 범위 (cost monitoring 은 향후
v22.5+ 에서 별도 검토).

## 관련 문서

- 직전 핸드오프: [`session-2026-05-01-0001.md`](session-2026-05-01-0001.md)
- v22.4 처방 큐 원본: [`session-2026-04-26-0003.md`](session-2026-04-26-0003.md)
- 6편 통합 분석: `.ai/research/harness-insights-2026-04-26.md`
- ratchet 메모리: `~/.claude/projects/.../memory/feedback_ratchet_rules.md`
