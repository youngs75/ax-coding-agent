# 세션 핸드오프 — 2026-05-01-0001: v22.4 4-처방 구현 + Xiaomi Mimo provider 통합

## 프로젝트 개요

0005 의 portal 통합 e2e *완전 정상* 마일스톤 도달 후, B 트랙 (v22.4 처방
큐 재개) 으로 복귀. 0426-0003 의 핸드오프에서 기록된 4가지 처방을 모두
구현하고 단위 테스트로 회귀 차단 확립. 추가로 사용자 결정으로 LLM
provider 를 Xiaomi Mimo (mimo-v2.5-pro / mimo-v2.5) 로 전환. 다음 단계는
v27 워크스페이스 e2e — Mimo 환경에서 v22.4 패치 4종이 함께 동작하는지
체감 검증.

## 작업 환경

0005 와 동일하게 `main` 브랜치 위. Docker 이미지 재빌드는 e2e 시점.
v22.4 패치는 모두 backwards-compat (TodoStatus 만 enum 확장 — 기존 값
영향 없음, libs 변경 없음). Mimo provider 도 add-only.

## 한 일

### 1. v22.4 #1 — `done-condition.md` 잘못된 예 강화 (planner skill)

`coding_agent/skills/planner/done-condition.md` 의 "잘못된 예" 섹션을
3분할 (글로브 형식 아님 / 자연어 조건 / 헤더 텍스트 다름). v25 회귀의
직접 케이스 (`**/requirements.txt must exist`, `**/package.json (if ...)`)
를 ❌ 로 명시 + 키워드 리스트 (`must`/`should`/`if`/`when`/`only`/
`except`/`필수`/`있어야`/`없어야`) 명문화. 끝 괄호 메모는 자유 허용.

### 2. v22.4 #2 — `_extract_forbidden_patterns` 자연어 거부 (sufficiency)

`coding_agent/sufficiency/signals.py` 의 `_extract_forbidden_patterns` 가
bullet 라인의 *괄호 밖* 텍스트에 자연어 키워드 (영어 7종 + 한국어 5종)
가 보이면 그 패턴 무효화. 괄호 안 메모 (`(React was chosen)`) 는 자유
텍스트로 허용 — false-positive 회피. 신규 `_NL_CONDITION_RE` +
`_PAREN_NOTE_RE`. log 는 debug-level (정상 동작).

### 3. v22.4 #1+#2 단위 테스트 — `tests/test_done_condition.py`

12개 테스트:
- 정상 패턴 통과 (3): pure glob, paren note 허용, asterisk bullet
- 자연어 거부 (5): must / should / if / 한국어 / when
- 경계 케이스 (4): paren-only allowed, mixed lines, no-header empty,
  next-h2 boundary

### 4. v22.4 #3 — fixer hard cap task_id-free 확장 (task_tool)

`coding_agent/tools/task_tool.py`:

```python
_FIXER_GATE_CAP_KEY = "__gate__"
```

`_on_start` 의 fixer 분기가 `task_id or _FIXER_GATE_CAP_KEY` 로 누적.
`_run_wrapped` 의 short-circuit 도 같은 sentinel 적용. INCOMPLETE 메시지에
`<gate-level>` 라벨 노출. v25 의 sufficiency.critic 발 task_id-free fixer
무한 loop 가 layer 별 안전망에서 추가로 차단됨 (loop_detection 외에).

테스트 4개 (`test_task_tool_evidence.py` 추가):
- task_id 기반 cap (기존 v22.1)
- 다른 task_id 격리
- gate-level cap (v22.4 핵심)
- gate-level vs task_id 격리

또 fake `_CapturingInnerTool.func` 가 실제 라이브러리처럼 `on_tool_call_start`
도 발화하도록 보강 (기존엔 end-only — 카운터 누적 안 됨).

### 5. v22.4 #4 — `verify_failed` status + auto-advance 시점 (todo+task+cli)

**`coding_agent/tools/todo_tool.py`**:
- `TodoStatus = Literal["pending", "in_progress", "completed", "verify_failed"]`
- `_VALID_STATUSES` 4-tuple 로 확장
- `_STATUS_GLYPHS["verify_failed"] = "[!]"`
- `replace` 가드: `completed` + `verify_failed` 모두 *terminal* 로 보호
- `update_todo` *tool* (LLM-facing) 은 `verify_failed` reject — harness-only
- `TodoStore.update` 직접 호출은 허용 (`_auto_advance_todo` 경로)
- `render_todo_summary` header 가 `verify_failed=N` 도 표시 (>0 일 때)

**`coding_agent/tools/task_tool.py`**:
- `_on_end` 의 coder COMPLETED 분기에서 `return` (advance 보류) — v25
  의 *CLI ✓ 의 거짓말* 차단
- `_run_wrapped` 의 coder COMPLETED → `_auto_verify_chain` 종료 후 result
  본문의 마커로 advance:
  - `_AUTO_VERIFY_PASSED_MARKER` → `completed`
  - `_AUTO_VERIFY_FAILED_MARKER` → `verify_failed`

**`coding_agent/cli/display.py`**:
- `_TODO_GLYPHS["verify_failed"] = ("⚠", "red")`
- title bar 에 `[red]N verify-failed[/red]` 표시 (>0 일 때)
- 각 row 는 `[bold red]` 로 강조 (사용자 검토 필요 신호)

테스트:
- `tests/test_todo_tool.py`: counts 4-key, terminal entries 메시지, `verify_failed`
  reject by tool / accept by store-direct (boundary 정책 테스트)
- `tests/test_task_tool_marking.py`: coder COMPLETED 단독 → advance 안 함,
  parametrize 에서 coder 분리
- `tests/test_task_tool_chain_advance.py` (NEW): chain PASS/FAIL/no-marker
  마커 5 시나리오. `_auto_verify_chain` monkeypatch 로 격리

### 6. Mimo provider 통합 (config + models + .env)

사용자 결정으로 LLM provider 를 dashscope → mimo 로 전환. Xiaomi 의
`token-plan-sgp.xiaomimimo.com/v1` (OpenAI 호환) 직접 호출.

**`coding_agent/config.py`**:
```python
mimo_api_key   = os.getenv("MIMO_API_KEY", "")
mimo_base_url  = os.getenv("MIMO_BASE_URL", "https://token-plan-sgp.xiaomimimo.com/v1")

_MIMO_MODELS = ModelTier(
    reasoning="mimo-v2.5-pro",
    strong="mimo-v2.5-pro",
    default="mimo-v2.5",
    fast="mimo-v2.5",
)
```

`model_tier` + `api_key` property 양쪽에 `mimo` 분기 추가.

**`coding_agent/models.py`**:
`elif cfg.provider == "mimo"` 분기에서 `disable_streaming=True` 부착.
mimo-v2.5* 가 모두 `reasoning_content` 반환하는 thinking 모델 — probe
(2026-05-01) 에서 "hi" 한 마디에도 reasoning_tokens=14 소모 확인.
fast tier 비용은 향후 모니터링 필요.

**`.env` / `.env.example`**:
- `LLM_PROVIDER=mimo`
- `MIMO_API_KEY=tp-sfdbr...`
- `MIMO_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1`
- 4-tier 모델 매핑 mimo-v2.5-pro / mimo-v2.5
- `.env.example` 에 [E] Mimo 옵션 등록

**호출 검증** (실 API):
- `mimo-v2.5` (fast): "Reply with just: OK" → "OK" + reasoning preserve
- `mimo-v2.5-pro` (reasoning): "7×8" → "56"
- `mimo-v2.5-pro` tool calling: `add(x=13, y=29)` → native tool_calls
  정상 emit (OpenAI 표준 호환 — quirky 처리 불필요)

### 7. 전체 회귀 테스트

`make test` 대신 `pytest tests/` 직접 실행 — **433 passed, 0 failed**.
신규 테스트 21개 (12 done_condition + 4 fixer cap + 5 chain advance) +
기존 보강.

## 현재 상태

| Repo | Local | Remote |
|---|---|---|
| ax-coding-agent | `main` ahead by 3-5 (이번 세션 commit 분) | `e22135b` (0005) |
| 작업트리 | clean post-commit 예정 | — |

활성 LLM: Xiaomi Mimo (mimo-v2.5-pro / mimo-v2.5).
Docker 이미지: 미빌드 (v27 e2e 시 빌드).

## 다음 단계 (사용자 작업)

### A. v27 워크스페이스 e2e (즉시) ★ 사용자 명시

```bash
mkdir -p /home/youngs75/temp/ax_pms_v27
cd /home/youngs75/temp/ax_pms_v27
~/projects/ax_advanced_coding_ai_agent/ax-agent.sh
```

검증 포인트:
1. **Mimo 호환성** — planner/coder 가 Mimo 로 안정 동작 확인. tool
   calling, multi-turn, reasoning_content preserve 모두 cross-check
2. **v22.4 #1+#2 효과** — DONE_CONDITION.md 가 자연어 조건 안 섞이고
   planner 가 깨끗한 glob 만 쓰는지 확인. 만약 섞여도 sufficiency 안전망
   에서 거부 + 정상 진행
3. **v22.4 #3 효과** — fixer 무한 loop 시나리오 (의도적 모순 요청) 에서
   gate-level cap 발화 확인. log: `task_tool.fixer_hard_cap_reached`
4. **v22.4 #4 효과** — coder COMPLETED 직후 todo 가 즉시 ✓ 안 되고,
   _auto_verify_chain 이 PASS / FAIL 판단 후 advance 되는지. fail 시
   `verify_failed` 표시 (CLI ⚠ red bold) 확인
5. **백엔드 vs 프론트엔드** — v25 의 *5/13 fail* 이 모두 React 런타임
   부재로 인한 격차였음. Mimo 가 백엔드 8/8 PASS 패턴 유지하는지

### B. fixer chain 검증 시나리오 (선택, 0005 에서 큐잉)

verifier 가 *fail* 하는 시나리오로 별도 e2e — 의도적 모순 요청 ("/todos
POST 가 200 반환하되 response_model 은 유지") 또는 spec 위반 ("test
없이 main.py"). v22.4 #4 의 verify_failed 마킹이 실제 발화하는지 직접
관찰 가능.

### C. 진단 logging cleanup (선택, 0005 에서 큐잉)

portal e2e 가 *완전 정상* 도달했으므로 `chat_common.js` 4 console.debug
+ `sse_emitter.py` `a2a.sse.event_in` info-level 을 환경변수 게이팅
(`AX_SSE_DEBUG=1` / `localStorage.chat_common_debug`) 으로 변경.

### D. minyoung-mah 0.2.0 PR (장기 — v22.4 #4 와 묶음)

핸드오프 0426-0003 의 승격 후보 B+C+E 묶음 — `verify_failed` status enum
을 라이브러리로 정식 추출. 현 ax 단독 구현은 forward-compat (라이브러리
스키마가 같은 enum 채택하면 변경 없이 호환). v23 정식 라운드 작업.

## 중요 참고사항

### v22.4 의 의미 — 6번째 책임 "Artifact Verification" 의 마지막 누수 봉합

v22 #2 의 auto-verifier chain 은 *호출* 까지 강제했지만 *결과 반영* 은
todo schema 의 한계로 거짓 ✓ 발생 가능. v22.4 #4 가 그 마지막 누수를
봉합 — 이제 chain 결과가 todo 표시까지 정확히 전파. R-001 (0 파일 +
completed=true 영구 금지) 의 *증거 기반* 강화.

### Mimo provider 의 thinking 모델 특성

- 모든 사이즈 (`v2.5` / `v2.5-pro`) 가 `reasoning_content` 반환
- `disable_streaming=True` 적용 (deepseek/zai 와 동일 워크어라운드)
- `enable_thinking=False` 같은 토글이 있는지 미확인 — fast tier 의 단순
  분류 작업도 reasoning_tokens 소모 → v27 e2e 비용 모니터링 필요
- 만약 비용 부담 시 추가 처방 옵션:
  - DEFAULT_MODEL / FAST_MODEL 만 다른 provider (e.g., dashscope qwen3.5-flash)
    로 hybrid
  - Mimo API 의 thinking off 옵션 정찰

### .env 변경분은 commit 제외 (.gitignore)

`.env` 자체는 .gitignore — API 키 노출 차단. `.env.example` 만 commit.
새 워크스페이스 클론 시 `cp .env.example .env` 후 `MIMO_API_KEY` 채우기.

### 본 세션 commit 단위 (예정 3 분할)

| # | 메시지 | 파일 |
|---|---|---|
| 1 | feat(harness): v22.4 — DONE_CONDITION 자연어 거부 + fixer cap gate-key + verify_failed status | task_tool/todo_tool/sufficiency/cli + skills/planner + 5 tests |
| 2 | feat(models): Xiaomi Mimo v2.5 provider 통합 | config.py + models.py + .env.example |
| 3 | docs(handoff): 세션 핸드오프 2026-05-01-0001 | 본 파일 |

## 관련 문서

- 이전 핸드오프: [`session-2026-04-30-0005.md`](session-2026-04-30-0005.md)
- v22.4 처방 큐 원본: [`session-2026-04-26-0003.md`](session-2026-04-26-0003.md) §"v22.4 처방 큐"
- Harness 6책임 통합 분석: `.ai/research/harness-insights-2026-04-26.md`
