# 세션 핸드오프 — 2026-04-27-0002: 포털 배포 최종 완료 (Opus 티어 + SSE rich spec + ALB fix)

## 프로젝트 개요
ax-coding-agent 를 사내 포털 EKS 에 최초 배포 완료. dummy 스텁 → AgentLoop
실연결 → SSE rich event spec + HITL interrupt-resume → ALB health check fix →
Opus 4-tier 매핑까지 한 세션에서 완수.

## 작업 환경
- **포털 Web IDE**: devcontainer (Debian 13, Python 3.12, Node 22, root)
- **GitLab**: `gitlab.samsungsdscoe.com/.../ax-coding-agent.git` (배포 트리거)
- **GitHub**: `github.com/youngs75/ax-coding-agent.git` (SSOT)
- **배포 endpoint**: `https://portal-serving-evangelist-1-agent2-e4e8d185.samsungsdscoe.com`

## 이전 세션 (0001) 이후 추가 작업

### SSE rich event spec + HITL interrupt-resume (`62a2260`, VDI 작성)
- `coding_agent/web/sse_emitter.py` 신규 — LangGraph events → A2A spec event 변환
- `/a2a/stream` 이 apt-web chat UI 용 이벤트 발행:
  `orchestrator.run.start/end`, `role.invoke.*`, `role.tool.call.*`, `todo.change`, `input_required`
- `/a2a/respond` 가 `session_id` 기반 Future resolve → LangGraph `Command(resume=...)` 재개
- 테스트 12/12 passed

### ALB health check 404 해소 (`feb2dd8`)
- 포털 ALB가 `GET /`로 health check → 404 반환하고 있었음
- `@app.get("/")` 추가하여 `/healthz`와 동일 응답

### Opus 4-tier 매핑 (ENV 주입, 코드 변경 없음)
- `REASONING_MODEL=us.anthropic.claude-opus-4-6-v1`
- `STRONG_MODEL=us.anthropic.claude-opus-4-6-v1`
- `DEFAULT_MODEL=us.anthropic.claude-sonnet-4-6`
- `FAST_MODEL=us.anthropic.claude-sonnet-4-6`
- 로그에서 tier별 모델 바인딩 확인 완료

## 현재 상태

### 배포 완료 — 정상 동작 중
```
tier=reasoning  model=us.anthropic.claude-opus-4-6-v1
tier=strong     model=us.anthropic.claude-opus-4-6-v1
tier=default    model=us.anthropic.claude-sonnet-4-6
tier=fast       model=us.anthropic.claude-sonnet-4-6
memory_store    db_path=/data/memory_store/ax.v2.db
ALB health      GET / → 200 OK
```

### 포털 배포 ENV 전체
| ENV | 값 |
|---|---|
| `LLM_PROVIDER` | `litellm_portal` |
| `REASONING_MODEL` | `us.anthropic.claude-opus-4-6-v1` |
| `STRONG_MODEL` | `us.anthropic.claude-opus-4-6-v1` |
| `DEFAULT_MODEL` | `us.anthropic.claude-sonnet-4-6` |
| `FAST_MODEL` | `us.anthropic.claude-sonnet-4-6` |
| `AX_PROJECT_ROOT` | `/data` |

### GitHub ↔ GitLab 동기화 상태
양쪽 모두 `feb2dd8` (최신, 동일)

### 커밋 이력 (이 세션에서 생성)
| SHA | 출처 | 내용 |
|---|---|---|
| `fe4e051` | 포털 IDE | 환경 정찰 보고서 |
| `d0ae974` | 포털 IDE | AgentLoop 실연결 + `AX_PROJECT_ROOT` env |
| `681d6cb` | 포털 IDE | 완료 보고서 + 세션 0001 |
| `62a2260` | VDI | SSE rich event spec + HITL interrupt-resume |
| `feb2dd8` | 포털 IDE | ALB `GET /` health check fix |

## 다음 단계

### 1. apt-web 통합 (VDI 작업)
- `.ai/apt-web-integration-v1-instructions.md` 실행
- `PORTAL_DEPLOY_REPORT.md` §8 반영 사항 주의:
  - SSE event 이름이 A2A spec 으로 변경됨 (§8.2)
  - `AGENT_CODING_BASE_URL` 설정 필요 (§8.3)
  - HITL respond flow 정합성 확인 (§8.4)

### 2. E2E 검증
- apt-web UI → ax-coding-agent 실제 코드 생성 작업 완주 테스트
- SSE 이벤트가 chat UI 에 정상 시각화되는지 확인
- HITL modal 라운드트립 검증

### 3. 경미한 개선 (낮음)
- `langfuse.langchain` 모듈 없음 warning → Dockerfile 에 `pip install langfuse[langchain]` 추가
- minyoung-mah 0.2.0 승격 (별도 사이클)

## 중요 참고사항

- **GitLab force push 불가** — merge push만 가능
- **Dockerfile 선택** — 포털이 `Dockerfile.portal` 사용하도록 지정 필요 (기본 `Dockerfile`은 kaniko 실패)
- **포트 8080** — dev pod 에서는 인증 프록시 점유, 배포 Pod 에서는 정상
- **LiteLLM 모델명** — prefix(`openai/`) 없이 사용 (`us.anthropic.claude-opus-4-6-v1`)
- **새 모델 추가 시** — 포털에서 `pending_approval` (관리자 승인) 발생 가능
