# Session Handoff — 2026-04-11-0001

## 프로젝트 개요
AX Coding Agent — 3계층 장기 메모리, 동적 SubAgent 수명주기, Agentic Loop 복원력을 갖춘 AI Coding Agent Harness.
AX Advanced 교육 Mini Project 산출물.

## 최근 작업 내역

### 1. 프로젝트 from scratch 구현 (완료)
- 참고 소스코드 3종 분석 (DeepAgents, Claude Code, Codex)
- 5개 병렬 트랙 (인프라 → 메모리/SubAgent/복원력 → 통합+CLI) 구현
- 47개 유닛 테스트 전부 통과

### 2. 오픈소스 모델 호환성 (완료)
- `tool_call_utils.py`: JSON args 3단계 복구, 고아 tool_call 정리, DashScope 직렬화
- `tool_adapter.py`: native tool calling ↔ 프롬프트 기반 자동 전환
- GLM-5를 STRONG(코딩) 티어로 설정 및 검증

### 3. Docker 배포 체계 (완료)
- Dockerfile + entrypoint.sh (HOST_UID/GID로 파일 권한 해결)
- docker-compose.yml (Agent + LiteLLM Gateway + Langfuse)
- 기존 LiteLLM Proxy 재활용 지원
- `ax-agent.sh` 실행 스크립트

### 4. CLI 개선 (완료)
- Claude Code 스타일 스트리밍 도구 호출 표시 (⚡/⇢/💾/✓/✗)
- `astream_events` 기반 실시간 출력
- JSON 메모리 블록 필터링
- 디버그 로그 파일 분리 (`.ax-agent/logs/agent.log`)

### 5. Agentic Loop 복원력 강화 (완료)
- `recursion_limit`을 `ainvoke`/`astream_events`에 직접 전달 (LangGraph 기본값 25 문제 해결)
- 연속 에러 3회 시 즉시 safe_stop (무한 루프 방지)
- `/resume` 기능 — safe_stop 시 `.ax-agent/resume.json` 저장, 다음 세션에서 이어서 진행

### 6. PMS 시나리오 E2E 테스트 (완료)
- 시스템 프롬프트를 Orchestrator 패턴으로 전환 (SubAgent 위임 강제)
- PMS 프로젝트 생성 성공: 151파일, 33,537줄, 48개 테스트
- 19회 SubAgent 위임 (planner + coder), GLM-5 96회 호출
- 총 비용 $0.38, 88분 소요

### 7. 문서화 (완료)
- README.md: 요구사항 매핑, 아키텍처, 배포 방법
- AGENTS.md: 루트 + 하위 디렉토리 8개
- Langfuse 트레이스 추출 유틸리티

## 현재 상태

### 코드 상태
- GitHub: https://github.com/youngs75/ax-coding-agent
- 최신 커밋: AGENTS.md 전체 생성
- 47개 테스트 전부 통과
- Docker 이미지 빌드 완료 (`ax-coding-agent:latest`)

### 모델 설정 (.env)
```
LLM_PROVIDER=openrouter
LITELLM_PROXY_URL=http://localhost:4000/v1
REASONING_MODEL=dashscope/qwen3-max
STRONG_MODEL=openrouter/z-ai/glm-5
DEFAULT_MODEL=dashscope/qwen3.5-plus
FAST_MODEL=dashscope/qwen3.5-flash
```

### PMS 산출물 위치
- `/home/youngs75/projects/new_pms/` — 151파일, 풀스택 PMS (backend + frontend)
- 백엔드: Node.js + Express + Prisma + PostgreSQL
- 프론트엔드: React + TypeScript + Material-UI + Redux + Frappe Gantt

### Langfuse 관측성
- LiteLLM Proxy 경유 자동 트레이싱 활성화
- 최근 100 트레이스: GLM-5 96회, Qwen Flash 4회, 총 $0.38

## 다음 단계

### 과제 제출 준비
1. README.md 최종 검토 — 요구사항 매핑 테이블 확인
2. PMS 산출물 빌드 테스트 (Node.js 환경에서 `npm install && npm test`)
3. 데모 시나리오 녹화 또는 스크린샷

### 남은 개선 사항
1. **로그 파일**: structlog WARNING 필터로 info/debug 누락 → 파일은 DEBUG로 기록하도록 수정
2. **CLI `⇢ 위임:` 표시**: SubAgent 위임이 `⚡ task`로만 표시됨 → `⇢` 아이콘 구분
3. **메모리 추출 빈도**: 매 턴 추출 → N턴마다 또는 토큰 임계값 기반으로 최적화
4. **GLM 타임아웃**: 긴 코드 생성 시 60초 타임아웃 → 120초로 확대 고려

## 중요 참고사항

### 알려진 이슈
- Docker 컨테이너 내부에 Node.js 미포함 → SubAgent가 자동으로 설치하지만 시간 소요
- GLM-5가 간혹 51K 토큰 컨텍스트에서 89초 소요 → 타임아웃 근접
- `astream_events`에서 SubAgent 내부 도구 호출이 메인과 동일 레벨로 표시

### 파일 구조 (핵심)
```
ax_advanced_coding_ai_agent/
├── coding_agent/
│   ├── core/loop.py              ← 메인 LangGraph 루프
│   ├── core/tool_adapter.py      ← 오픈소스 모델 tool calling 어댑터
│   ├── core/tool_call_utils.py   ← JSON 복구, 고아 정리
│   ├── memory/                   ← 3계층 메모리 (SQLite+FTS5)
│   ├── subagents/                ← 8상태 FSM, 동적 팩토리
│   ├── resilience/               ← 7가지 장애 처리
│   ├── tools/                    ← 파일/셸/task 도구
│   ├── cli/                      ← 스트리밍 CLI
│   └── utils/                    ← Langfuse 유틸리티
├── tests/                        ← 47개 테스트
├── Dockerfile, docker-compose.yml, ax-agent.sh
└── .env                          ← API 키, 모델 설정
```
