# Session Handoff — 2026-04-19-0002

## 프로젝트 개요

AX Coding Agent — `minyoung-mah` 라이브러리의 세 번째 소비자. harness 5책임
(Safety/Detection/Clarity/Context/Observation) 중 coding-domain 특화
부분만 이 repo 에 남음.

**이번 세션 성격** — Claude 4.6 세대로 주력 tier 를 교체한 E2E 실증 +
bias/격리 결함 전수 진단 세션. 직전 세션(0001) 에서 닦은 Phase 1-9
리팩토링 위에서 "더 강한 instruction-following LLM 으로 바꾸면 뭐가
드러나나" 를 관찰하는 통제된 실험.

## 이번 세션 작업 흐름

### 1. v10-1차 회귀 핫픽스 — commit `a1268b3`
직전 세션 말미 E2E 에서 TASK-01 만 완료 후 `tool_calls=None` 으로 종료된
현상의 근본 원인 확정 → 수정 → 푸시.

근본 원인: verifier free-form 자연어 ("## Error Report / ## Fixer Instructions /
## Success Criteria" 마크다운 섹션) 가 `_format_verifier_output` 을 통해
최상위 orchestrator input 에 흘러들어가 LLM 을 continuation 모드로
빠뜨렸음. 라이브러리 버그 아닌 ax pre-existing 설계 취약점
(v9 완주는 샘플링 운이었음).

수정:
- `_sanitize_verifier_text` 신설 — forbidden heading 앞까지 유지 + 400자 truncate
- `_VERIFIER_PROMPT` 에 instruction-style 섹션 작성 금지 지시 추가
- `fixer_role` tool_allowlist 에 `write_file` 추가 + prompt 업데이트

187/187 테스트 통과 후 푸시.

### 2. v10-2차 qwen E2E — 성공적 개선 확인, 새 병목 노출
- `tool_calls=None` **완전 해소** (전체 구간에서 0건)
- 14/15 task 완주 + `max_iterations=50` SafeStop 으로 종료
- resume.json exit_reason=`safe_stop`, progress_summary="TASK-15 로 마무리하겠습니다"
- **update_todo 연타 관찰** — iter 50 중 16 iter 이 ledger 업데이트 (연타 패턴)

사용자 지적: **"이터레이션 증가는 근본 해결 아님. Langfuse 로 입출력 확인해라."**

### 3. Claude 계열 전환 — 통제된 비교 실험
- `.env` 의 Claude 4.6 프리셋 활성 (opus-4-6/sonnet-4-6/haiku-4-5)
- 첫 Claude E2E (v10-3차) 에서 **같은 tool_calls=None 재발** — TASK-01 만 완료 후 중단
- Langfuse trace 추출 (iter 15 trace `db123195`)
- 결정적 단서: output 말미에 `</parameter></function></tool_call>` XML 닫는 태그가 content 로 흘러나옴
- **근본 원인 확정**: `ChatOpenAI(...)` 에 `max_tokens` 미설정 → LiteLLM 이 Claude 에 작은 default 주입 → 큰 `write_todos` tool_use 응답이 중간에 잘림 → 파싱 실패

### 4. 구조적 수정 (본 세션 uncommit → 이번 핸드오프와 함께 커밋)

| 파일 | 변경 | 해결하는 결함 |
|---|---|---|
| `coding_agent/models.py` | tier 별 `max_tokens` (32K/32K/16K/8K) | Claude tool_use clip |
| `ax-agent.sh` | workspace 경로 md5 → `AX_PROJECT_ID` + `ax-agent-memory` volume mount | 메모리 교차 오염 방지 + persistent 메모리 |
| `docker-compose.yml` | `AX_PROJECT_ID` env passthrough | 상동 |
| `coding_agent/config.py` | `project_id` 필드 (AX_PROJECT_ID env) | 상동 |
| `coding_agent/core/loop.py` | `initial_state.project_id` 기본값 = `cfg.project_id` | 상동 |
| `coding_agent/cli/app.py` | 상동 | 상동 |
| `coding_agent/memory/middleware.py` | 모든 tier(user/project/domain) 에 `scope=project_id` 격리 | domain tier 의 전역 공유 차단 |
| `Dockerfile` | `minyoung_mah` BuildKit named context | 빌드 재현성 |

### 5. Claude E2E 재실행 — 15/15 완주 + 자율 재출발 현상 발견
수정 반영 후 재실행 결과:
- `tool_calls=None` 재발 **없음** (max_tokens 수정 효과 입증)
- 15/15 task 완주
- 하지만 완주 **직후** orchestrator(Claude sonnet) 가 **새로운 20-task SPEC 을 `write_todos` 로 덮어쓰고 TASK-01 부터 재출발**
- `SYSTEM_PROMPT` 에 "모든 todo 완료 시 자연어 요약으로 종료" 지시 없음 → Claude 는 "다음 할 일 찾기" 로 해석
- `write_todos` 가 destructive replace → 이전 15 completed 기록 소실
- 사용자 "이건 명백한 하네스 잘못" 판단 → `docker kill ax-agent`

### 6. Hang 진단 — CLI 카운터 착시 정정
- 사용자 지적 "orchestrator 가 혼자 착각해서 돌고 있나?" 에 초기 가설 (memory extract) 제시했으나 **LiteLLM proxy 에 해당 호출 없음** 으로 반증
- Python main state `S (sleeping)` + ESTABLISHED TCP 0 → 실제 LLM 호출 in-flight 아님
- 자식 프로세스 `esbuild × 8 + node × 3 + npm install` — execute tool 이 spawn 후 detach 한 serve/watch 류 고아
- `py-spy dump` 시도 → ptrace 권한 거부로 정확한 stack 확보 실패
- CLI `step N` 라벨 = `on_chat_model_start` 이벤트 카운터 (모든 LLM 호출 누적) — agent_node iter 수와 다름

### 7. Bias 전수 조사 (사용자 원칙 적용)
사용자 원칙: *"특정 기술/기능/순서를 쓰게 하거나 따르게 하는 bias 가 생기면 안 된다."*

직전 커밋 `a1268b3` 에서 제가 verifier 에 "go, node, apt-get, curl" / "compile, lint, type-check" 등 예시를 넣고 "## Error Report / Fixer Instructions" forbidden list 를 prompt 에 넣은 것도 원칙 위반으로 확인 → 철회 대상.

전수 조사 결과는 §중요 참고사항 에 목록.

## 현재 코드 상태

- **브랜치**: main
- **커밋 전 상태**: 8 파일 변경 (§4 목록) — 이 핸드오프 파일과 함께 커밋 예정
- **직전 푸시**: `a1268b3` (verifier 자연어 오염 차단)
- **테스트**: 187/187 통과 (§4 수정 반영 후)
- **Docker**:
  - `ax-agent` 컨테이너 stopped (docker kill)
  - `ax-litellm-proxy`, `ax-litellm-db` 계속 up
  - `ax-agent-memory` volume 에는 첫 Claude E2E 의 메모리가 남음 (PROJECT_ID `72bdd46e0a8f` = `/tmp/ax_pms_e2e_v4`)
  - 이전 고아 volume `docker_memory_data` 는 이미 삭제됨

## 🔴 다음 세션 ToDo (우선순위 순)

### P1 — 완료 조건 부재 (harness 결함)
- [ ] `SYSTEM_PROMPT` (`core/loop.py`) 에 **종료 조건 명시**:
      pending=0 && in_progress=0 이면 즉시 자연어 요약 응답으로 끝낸다.
      새 write_todos / task 위임 금지.
- [ ] `write_todos` tool 에 **파괴적 replace 방어**:
      ledger 에 completed 항목이 있는데 replace 요청 오면 거부 또는 append-only 변환.
      현재는 TodoStore.replace() 가 전체 덮어쓰기 (tools/todo_tool.py).

### P2 — update_todo 연타 감소
qwen/Claude 모두 coder 한 번에 update_todo 2-3회 명시 호출. `SYSTEM_PROMPT`
의 "harness 자동 마킹 신뢰" 지시가 약하다.
- [ ] `task_tool._auto_advance_todo` 를 **verifier COMPLETED + execute ok=True 일 때도 completed 로 승격**.
      현재는 coder 만 승격, verifier/fixer 는 in_progress 유지.
- [ ] 또는 `update_todo` tool 자체를 orchestrator binding 에서 제거.
      장점: iter 낭비 제거, bias 감소. 단점: auto-advance 미처리 케이스에서 ledger 가 영원히 in_progress.

### P3 — Bias 제거 (사용자 원칙)
`_PLANNER_PROMPT` (`subagents/roles.py:58-86`):
- [ ] Line 69-70 "tech stack, **auth scope**, target platforms, **storage**, **deployment**, scope boundaries" ambiguous 예시 삭제
- [ ] Line 73 "Save ... under **docs/** (e.g. **docs/PRD.md, docs/SPEC.md**)" 파일 경로 강제 삭제
- [ ] Line 74-75 "Do not add **RBAC/SSO/analytics/dark mode/i18n**/etc" 금지 리스트 삭제
- [ ] Line 71 "Bundle **2–4** questions in one call" 개수 제약 삭제
- [ ] Line 76-77 "Do not combine multiple artifacts in one delegation" 재검토 (bias 성격)
- [ ] Line 83-84 "a methodology hint, a naming convention, a **deployment target**" 예시 삭제

`_VERIFIER_PROMPT` (이전 커밋 `a1268b3` 의 내 추가분):
- [ ] "install/configure dev tools (**go, node, apt-get, curl**, etc.)" 예시 삭제
- [ ] "**compile, lint, type-check** if applicable" 예시 삭제
- [ ] Forbidden headings 리스트 재검토 — prompt 에 두지 말고 **코드 레벨 `_sanitize_verifier_text` 만 유지** (이건 harness Safety 책임이라 OK)

`SYSTEM_PROMPT` (`core/loop.py:83-141`):
- [ ] Line 136 "모호한 요구사항(**기술 스택/플랫폼/인증 범위/저장소** 등)" 예시 삭제
- [ ] Line 99-132 의 "1. PRD 위임 → 2. SPEC 위임 → 3. Todo 등록 → 4. 구현 루프 → 5. 검증" **5단계 워크플로 강제** 완화/삭제 논의
      현재는 "사용자 요청이 PRD/SPEC 불필요한 경우" 에도 planner 를 먼저 부르게 유도. 대안: role 목록 + 원칙만 남기고 시퀀싱은 LLM 에 위임.

`_FORK_RULES` (`subagents/roles.py:26-50`):
- [ ] "under **500 words**" 분량 강제 삭제
- [ ] `Scope/Result/Files changed/Issues` 포맷을 계속 강제할지 재검토.
      `_format_verifier_output` / `_extract_written_files` 가 이 포맷에 의존하지 않음 (tool_calls/tool_results 구조 사용) — **제거 가능**할 것으로 추정.

### P4 — Langfuse observer SDK 호환성
- [ ] `langfuse_observer._handle` 의 `self._client.span(...)` / `self._client.event(...)` 가 매 이벤트마다 exception.
      최신 Langfuse SDK 기준 API 확인 후 수정.
      현재: fail-silent 로 메인 flow 영향 없음. 하지만 매 이벤트마다 `log.exception` 으로 파일 로그 폭증.
- [ ] 부작용: `agent.log` 파일이 iter 2 부근에서 멈춘 것으로 관찰됨 (file flush 이상) — observer 예외 처리 중 structlog 이 blocking 했을 가능성 조사.

### P5 — execute tool cleanup
- [ ] `execute` 가 spawn 한 **grandchild 프로세스 정리**:
      현재 coder 가 `npm run dev` 같은 serve 명령 실행 시 timeout 도달해도 grandchild(esbuild 등) 는 고아로 컨테이너에 남음.
      Process group 기반 kill (setpgid + killpg) 로 변경.
- [ ] 혹은 serve/watch 류 명령 **검출/거부** (단 bias 성격이라 원칙과 긴장).

### P6 — Observability 개선
- [ ] CLI `Orchestrator · step N` 라벨 의미 변경 또는 개명.
      현재 `on_chat_model_start` 이벤트 카운터 (모든 LLM 호출 누적) — agent_node iter 와 다르지만 사용자가 iter 로 오해함.
- [ ] 로그 파일 flush 문제 — 컨테이너 stdout 은 갱신되는데 `/workspace/.ax-agent/logs/agent.log` 는 특정 지점에서 멈춤.

### P7 — 이번 커밋 수정의 검증 확장
- [ ] AX_PROJECT_ID 격리가 실제로 작동하는지 **2개 workspace 로 E2E** — 한 workspace 에서 생성한 메모리가 다른 workspace 에 주입되지 않는지.
- [ ] `max_tokens=32768` 이 qwen3-coder-next 쪽에 악영향 없는지 확인 (현재 STRONG tier 공통 적용).

## 중요 참고사항

### LLM 선택 관점
- `.env` 현재 Claude 4.6 세대 프리셋 활성 (opus-4-6/sonnet-4-6/haiku-4-5).
  qwen 프리셋은 주석 처리됨. 같은 파일에서 토글로 복귀 가능.
- 다음 세션 bias 수정 후엔 qwen 으로 다시 돌려 **교차 검증 권장**.

### LLM 모델 별 최대 출력 토큰 (2026-04 기준 확인)
- `claude-opus-4-6`: 128K (beta 헤더 필요 없이)
- `claude-sonnet-4-6`: 64K
- `claude-haiku-4-5`: 64K
- 현재 ax 는 여유 있게 32K/32K/16K/8K 로 제한 (write_todos 14개 todo + verifier 큰 출력 수용에 충분).

### Claude 전환으로 드러난 구조적 인사이트
Claude 의 정확한 instruction-following 덕분에 기존 ax 가 "qwen 의 느슨함에 기대던" 몇몇 결함이 가시화:
1. `max_tokens` 미설정 → tool_use clip
2. 완료 조건 부재 → 새 SPEC 자율 재출발
3. update_todo 연타 — 이건 **LLM 무관** 한 harness 결함
4. Planner 의 `deployment` 예시 bias → Claude 가 AWS/Docker/CI-CD 를 강하게 연상 (qwen 은 무시했음)

즉 **Claude 호환성 수정은 동시에 qwen 의 숨은 결함을 드러내 고치는 기회**. 양방향 이득.

### LLM 이 Bias 를 따르는 강도 차이
prompt 에 예시·금지리스트가 있으면 instruction-following 강도 순서:
Claude > qwen3-max > qwen3-coder-next > qwen3.5-flash.
원칙에 따라 prompt 에서 특정 기술·기능·순서 언급을 전부 삭제하면
모든 LLM 이 사용자 요구사항만 충실히 반영하게 됨.

### 메모리 저장소 진실
- 이전 세션(0001) 의 내 분석 "metadata DB 가 교차 오염된다" 는 **절반만 맞음**.
  실제로는 `ax-agent.sh` 가 volume mount 를 안 붙여서 매 실행마다 컨테이너 내부 DB 가 새로 만들어지고 `--rm` 으로 사라졌음.
  즉 **실제 교차 오염은 없었지만 메모리 재사용도 불가능한 상태** 였음.
- 이번 세션 수정으로:
  - `ax-agent-memory` volume 마운트 추가 → persistent 메모리 복원
  - `AX_PROJECT_ID` (workspace md5 12자) 주입 → 모든 tier 에서 scope 격리
  - 같은 workspace 재방문 시에만 이전 메모리 주입. 다른 workspace 는 격리.

### Docker volume 상태
- `ax-agent-memory`: 첫 Claude E2E 데이터 포함 (PROJECT_ID `72bdd46e0a8f`).
  다음 세션에서 새 workspace 로 E2E 돌리면 `ax-agent-memory` 에 별도 project_id scope 로 저장됨.
  교차 오염 X.
- 혹시 이 volume 도 깔끔히 리셋하려면: `docker volume rm ax-agent-memory`.

### Claude 비용 참고
이번 세션 Claude E2E 2회 총 비용 Langfuse UI 에서 확인 가능 (span forward 실패로 수치 누락 가능성 — `langfuse_observer` SDK 호환 수정 후 정확해질 것).

### 플랜 파일 (참고)
- `/home/youngs75/.claude/plans/resilient-imagining-sedgewick.md` — 직전 Phase 1-9 리팩토링 계획.
  이번 세션의 결함들은 그 플랜 §결정/§핫스팟 과 독립된 새 발견.

### 다음 세션 시작 명령 (예시)
```bash
# 1. LLM Gateway 상태 확인
docker ps --filter name=ax-litellm

# 2. 라이브러리 최근 변경 확인 (소비자 영향)
git -C ../minyoung-mah log --oneline -5

# 3. 이번 커밋 내용 검토
git log --oneline -3

# 4. P1 부터 착수 — SYSTEM_PROMPT 완료 조건 명시
#   (bias 제거는 P3 에서 일괄 처리 — 동시에 하면 변수 통제 불가)
```

## 이번 세션 푸시 대상 (커밋 2개)

1. **fix: Claude 호환성 + 프로젝트 단위 메모리 격리** — §4 의 8 파일
2. **docs: 세션 핸드오프 0002** — 이 파일

둘 다 `origin/main` 로 푸시 (git 계정 `youngs75`).
